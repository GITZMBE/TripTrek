{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": { "commands": ["prisma generate && npm run build"] }
    }
  ],
  "routes": [
    { 
      "src": "/.*", 
      "dest": "serverless" 
    }
  ]
}
